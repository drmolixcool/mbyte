# Configuration OIDC (dev)
VITE_OIDC_AUTHORITY=https://auth.mbyte.fr/realms/mbyte
VITE_OIDC_CLIENT_ID=mbyte-gui
VITE_OIDC_SCOPE=openid profile email

# Base URL of the manager API.
# In dev, the GUI runs on http://localhost:5173 so the manager must allow CORS for that origin
# (including responding to OPTIONS preflight with 200/204 and the proper Access-Control-* headers).
VITE_API_MANAGER_BASE_URL=https://manager.mbyte.fr/

# Store routing
VITE_STORES_DOMAIN=s.mbyte.fr
VITE_STORES_SCHEME=https
